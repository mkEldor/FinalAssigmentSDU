<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="adilbek.finalassigmentsdu">

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">
        <activity
            android:name=".FragmentsMaster"
            android:configChanges="orientation|keyboardHidden">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity
            android:name=".LandSpaceFragmentMaster"
            android:configChanges="orientation|keyboardHidden" />
        <activity android:name=".VerstFacultyAndSduInfo" />
        <activity android:name=".VerstNewListFragment" />
        <activity android:name=".VerstContactFragment" />
        <activity android:name=".VerstDetailFragment"></activity>
    </application>

</manifest>